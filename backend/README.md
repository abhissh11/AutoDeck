# AI Slide Builder — Backend (Express)

This is a minimal Express.js scaffold (JavaScript) for the AI Slide Builder project.

Quick start

1. cd into the backend folder
2. npm install
3. npm run dev (requires nodemon) or npm start

API endpoints

- GET / -> basic welcome
- GET /api/health -> health check
